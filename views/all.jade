extends ./layout.jade

block content
    each post in blogs
        .post-container
            h2
                a(href= post.url)= post.title
            .post-preview!= post.preview
            .post-footer
                h3= post.relativeDate
                each tag in post.tags
                    .tag= tag
